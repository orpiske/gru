os:
- linux
sudo: true
language:
- c
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y cmake doxygen
before_script:
- mkdir build
- cd build
- cmake -DCMAKE_BUILD_TYPE=RELEASE ..
script:
- make all
